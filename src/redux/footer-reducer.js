let initialState = {
  copyright: "Almost made in the Netherlands",
};

export const footerReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};